# Lesson 2: JavaScript Before React

## Governing Principle
React is a JavaScript library. To engineer resilient, high-volume interfaces, we must master the underlying runtime machinery: lexical scope, closures, the Call Stack, Microtask Queue, Task Queue, the Event Loop, Promises, `async`/`await`, and `AbortController` cancellation.
